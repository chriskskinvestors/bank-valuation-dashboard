# Full Code Audit — 2026-07-02

Audited at origin/main `055689b` by 7 parallel specialist passes (SEC/extraction data layer, market-data/news layer, UI layer, jobs/deploy infra, security, tests/analysis/tooling, prior-audit verification). Every finding below was traced to a concrete failure scenario in the code before being reported. Known-intentional behaviors (square corners, reconcile gates, observe-only smoke, excluded tickers, n/a-by-design holdco walk, etc.) were excluded up front.

**Status legend:** every item is OPEN unless marked otherwise. Severity: P0 = wrong numbers rendering now on real banks; P1 = serious (wrong numbers on common paths, silent data loss, ops exposure); P2 = moderate; P3 = minor/latent.

---

## P0 — wrong numbers rendering now

1. **[DONE 2026-07-06 — commit e7b54c8, deployed green]** **TRADE (trading account ASSETS) rendered as "Trading account income"** — `ui/financials_statements.py:1195` (also 1200-1201, 465-477). Income Statement spec shows the balance-sheet field TRADE as a dollar income line, and the `noniother` residual computes Other non-interest income = NONII − (TRADE + fees). Any bank with a trading book shows a ~$B asset balance as quarterly income and a hugely negative "Other non-interest income", with a provenance popup citing "FDIC field TRADE". TRADE=0 community banks masked it during spot checks. Fix: remove TRADE from the IS spec / residual; the correct income field is TRADEINC-class (verify SDI field).

2. **[DONE 2026-07-06 — commit e7b54c8, deployed green]** **ELNATR (provision) labeled "Net Charge-Offs ($K)"** — `ui/historicals.py:48`. Renders in Quarterly Detail and Annual Summary. Everywhere else ELNATR = provision (config.py:895, financials_statements.py:1192). The NCO dollar field is NTLNLS. Reserve-building banks show a large fake NCO figure.

## P1 — serious

### Data layer
3. **[DONE 2026-07-06 — commit a6e86d3; get(max_age_s) + snapshot readers + no empty-memoize, pinned by tests/test_cache_stale_fallback.py]** **Global 24h TTL in `cache.get()` silently kills the universe snapshot's designed stale-fallback** — `data/cache.py:73` + `data/bank_universe.py:224-260` + `data/bank_mapping.py:417-430`. A snapshot >24h old reads as *missing*, not stale, so "serve whatever its age" can never trigger; one failed/late nightly refresh puts the ~6.5-min live build on the request path (the 2026-06-13 hang mode). Compounding: `bank_mapping._universe_snapshot_map()` memoizes the empty result module-globally and never retries.
4. **[DONE 2026-07-06 — commit 60da31c; both adapters scan their full lookback, pinned by TestBackstopIgnoresSince]** **Per-CIK 8-K backstop blinded by shared `since` cutoff** — `data/events/sec_8k.py:105,149-154` + `jobs/poll_events.py:185`. Fast adapter writes intraday timestamps; per-CIK adapter stamps midnight UTC under the same source name and `break`s on the first filing < since. The can't-miss full poll ingests ~nothing; 10-K/10-Q events (only emitted by the per-CIK path) never fire `_invalidate_fundamentals_for_filings`, so fundamentals ride the full 24h TTL after new filings.
5. **[DONE 2026-07-06 — commit 99bc384; per-common-share conventions ported (preferred ladder + cardinal rule + placeholder-count guard), grid key v2, USB-verified live; MSR/combined-intangible netting still diverges from the snapshot path (see #22 note)]** **Trends BVPS/TBVPS ignore preferred stock** — `data/sec_per_share.py:48-67`. Total equity (preferred included) ÷ common shares overstates TBV/share for every preferred issuer (USB ≈ +$4/sh, ~10-15%); also skips the rounded-share-count guard and combined-intangibles/MSR resolution the main path applies. All-universe (pre-warmed by refresh_trends).

### UI / valuation
6. **[DONE 2026-07-06 — commit daefb76]** **Valuation model consensus pre-fill ×4 on annual periods** — `ui/valuation_model.py:304-306`. `base_eps = value * 4` assumes quarterly, but `list_consensus` includes annual broker periods → 4× EPS seeds DCF/warranted/blended fair value and the verdict banner. Same flaw for payout.
7. **[DONE 2026-07-06 — commit daefb76]** **Model-vs-Consensus EPS row compares quarterly model EPS to annual street EPS** — `ui/valuation_model.py:891-892` (÷4 regardless of period type) → Δ ≈ −75% and a confident wrong verdict.
8. **[DONE 2026-07-06 — commit daefb76; all other highlight chips verified direction-correct + pinned against config color_rules]** **"Biggest discount" highlight chip inverted** — `ui/peer_comparison.py:86`. `("Biggest discount", "ptbv_discount", "min")` but ptbv_discount is positive-when-undervalued → chip names the MOST OVERVALUED bank, contradicting the table.
9. **[DONE 2026-07-06 — commit daefb76; shared Hazen percentile + missing spokes omitted]** **Radar chart plots missing metric as 0th percentile (worst-in-group)** — `ui/peer_comparison.py:957-959`. `None → 0`; also uses its own strictly-below percentile instead of shared Hazen `compute_peer_percentile`, so radar and table disagree for the same bank.
10. **[DONE 2026-07-06 — commit daefb76; single _subtab_key builder + divergence-guard test]** **Deep-link/refresh restore lands on wrong sub-tab in flat sections** — `app.py:475` writes `company_subtab::{section}` but the widget key is `company_subtab::{section}::None` (`app.py:1569`). Breaks `?tab=` restore for every non-first leaf of Valuation, News & Filings, Ownership.
11. **[DONE 2026-07-06 — commit daefb76; owner chose annualize-the-number ((1+QoQ)^4−1)]** **"Annualized Growth Rates (%)" not annualized in Quarterly view** — `ui/financials_statements.py:993-1011, 1311-1315`. Raw QoQ under an "Annualized" header (~4× off).
12. **[DONE 2026-07-06 — commit daefb76; 2-point _avg denominators]** **ROATCE/ROATE/ROACE "avg equity" computed on period-END equity** — `ui/financials_statements.py:510-519, 551-584`. Labels and provenance popups literally print "Avg …" that is never computed; sibling kinds (netopex, costfunds) use `_avg` correctly.
13. **[DONE 2026-07-06 — commit daefb76]** **Earnings "Biggest Beats/Misses" $-LaTeX garble** — `ui/earnings.py:1513-1525`. Two unescaped `$` amounts per line (built at 1439-1440); the parallel pattern at :285 is escaped.
14. **[DONE 2026-07-06 — commit daefb76; buckets by fiscal quarter]** **Surprise heat-map columns are 8 announcement DATES, not 8 quarters** — `ui/earnings.py:789-830`. Universe-wide date sort captures ~one earnings season; banks join by exact date so most populate one cell.
15. **[DONE 2026-07-06 — commit daefb76; YTD-labeled in-progress year + last-quarter ratios]** **Historicals: in-progress year rendered as full annual column** — `ui/historicals.py:93-122` (Q1-YTD next to FY under "Annual figures" = fake earnings collapse); and **:102** averages point-in-time (CET1, NPL) and YTD-annualized (ROA/NIMY) ratios across quarters instead of year-end/Q4.
16. **[DONE 2026-07-06 — commit daefb76; incl. the :1070 vline truthiness P3]** **Curve-slope card says "Inverted" when slope is None (FRED outage) or exactly 0.0** — `ui/rate_sensitivity.py:146-148` (truthiness chain; None fallback set at 195-202).

### Ops / tooling
17. **[FULLY CLOSED 2026-07-06 — commits a792337 + 5e38855, deployed green. Phase 1: the 4 code-proven jobs (premarket/live-yields/avg-volume/insider) added to both lists, live Verify passed. Phase 2: `gcloud scheduler jobs list` confirmed refresh-capital (2 triggers, 0 7 * * *) + refresh-macro (*/30) fire on Scheduler yet sat outside the guard → both added to the Ensure+Verify loops; deploy daefb76 ran the live Verify gate green ("All scheduled jobs carry the invoker binding"). refresh-company-financials is NOT scheduled → correctly excluded, do not re-open.]** **Scheduler-invoker guard misses ≥4 scheduled jobs** — `deploy.yml:214, :237`. refresh-premarket, refresh-live-yields, refresh-avg-volume, refresh-insider all provably have Cloud Schedulers (tools/create_home_jobs.py:27-45, tools/create_premarket_job.py) but are absent from the Ensure/Verify lists — the exact 2026-06 PERMISSION_DENIED silent-staleness incident class. refresh-macro/capital/company-financials likely scheduled too (confirm with `gcloud scheduler jobs list`). Fix: extend both lists.
18. **[DONE 2026-07-06 — commit 5c26c07, deployed green]** **`tools/resolve_all_mappings.py:298-303` unconditionally overwrites curated `data/bank_map_resolved.json`** with heuristic re-resolution — no merge/backup; a naive rerun reverts hand-curated wrong-entity fixes and §12(i) cik=None entries → wrong-entity FDIC joins. Contrast discover_otc_tickers.py:174-225 which merges correctly. Fix: merge-preserve + require an explicit flag.

## P2 — moderate

### Data correctness
19. **[DONE 2026-07-06 — pinned by tests/test_estimates.py]** **yfinance failure payloads cached as fresh for 6h** — `data/estimates.py:56-66, 157-168`. Error dicts stamped `cached_at` and persisted; throttled tickers drop out of the earnings calendar until expiry. Violates "failures are never cached". Fix: `fetch_estimates` returns an `{"error":...}` payload un-persisted (no `cached_at`, no `save_json`) so the next call retries; `_is_fresh_data` also rejects any error payload persisted by an older build.
20. **[DONE 2026-07-07 — pinned by tests/test_form13f_matching.py]** **13F substring issuer matching pollutes holder data** — `data/form13f_client.py:168-171`. `ticker in name` (KEY→KEYSIGHT, BANC→BANCOLOMBIA) when `company_name` omitted; wrong rows persist into merge-only quarter snapshots. Fix: extracted `_issuer_matches()` — a bare ticker must equal a whole alphanumeric word token of the issuer name (re.findall boundary), never a substring; multi-word company-name search terms keep exact-phrase substring behavior; full-name equality fast-path. 6-case test.
21. **[DONE 2026-07-09 — pinned by tests/test_adapters_ignore_since.py (8 cases, one per adapter)]** **`since = MAX(published_at)` drops late-syndicated news permanently** — `jobs/poll_events.py:185` (systemic, all adapters). Dedup already protects re-ingest; the cutoff only buys efficiency while losing items syndicated late or missed on a failed per-ticker fetch. Fix: extended the P1 #4 house pattern (sec_8k, 60da31c) to ALL remaining adapters — every adapter now ignores `since` and re-scans its own full LOOKBACK window (wires 7d, FMP/Yahoo 14d, Google News 3d, topics 48h, IR 30d); the runner no longer computes `since` at all. Zero API-budget cost: every adapter's cutoff only ever filtered POST-fetch (RSS feeds, FMP limit=250 batches, and per-ticker queries pull the same payload regardless), so the re-scan is pure no-op inserts absorbed by (source, external_id) + content-key dedup. The orphaned `store.last_seen_published` was deleted (which also moots its P3 SQLite-str-return note below).
22. **[DONE 2026-07-09 — pinned by TestProvenanceMatchesDisplay (trace==display parity)]** **Provenance path computes different numbers than the display path** — `data/sec_client.py:879-887, 903-943`. Trace tool shows single-period NI where app shows TTM (~4×), and its TBVPS skips `_resolve_intangible_adjustment` (BKU-class filers: TBVPS==BVPS). Fix: `get_fundamentals_with_provenance` now (1) serves TTM net income via the display path's `_extract_ttm_value` + same concept chain (the single filed period stays traceable under `net_income_latest_period`; no honest TTM → None, matching the A21 invariant), (2) computes TBVPS through `_resolve_intangible_adjustment` (combined-tag + MSR netting, exposed as a traced `intangible_adjustment`), and (3) traces the LADDER-resolved preferred value the math actually uses (was: wrapped only PreferredStockValue, contradicting the derived numbers for BAC/USB/JPM par+APIC filers).
23. **[DONE 2026-07-09 — commit 8ac8b4c, deployed green; pinned by test_no_prior_year_balance_yields_na_roa_roe]** **`extract_performance` "average" falls back to single period-end balance** — `data/sec_filing_scraper.py:1909-1924`. Missing prior-year instant → ROA/ROE on ending balance, flagged identically to a real average. Fix: the avg helper returns None when no prior-year instant exists (no true (begin+end)/2 formable), so ROA/ROE go n/a rather than a mislabeled single-point ratio; the faithfully-tagged income lines still render.
24. **[DONE 2026-07-06 — owner chose INTAN (total intangibles); pinned by TestValuationEngineTceConvention]** **TCE convention split across engines** — `analysis/valuation.py:142,218` (EQTOT − INTANGW, goodwill only) vs `ui/financials_statements.py:499-519` (INTAN, total intangibles) vs `analysis/capital_dynamics.py:50` (max of both). Same bank, same quarter, different ROATCE by page. Fix: valuation.py (the lone outlier) aligned to INTAN in both `compute_roatce` and `compute_roatce_4q`; the other two already used total intangibles (capital_dynamics' `max(goodwill,intangibles)` == INTAN in practice). Now all FDIC engines match the Financials tab, the golden hand-check, and CLAUDE.md. 24b. **[DONE 2026-07-07 — pinned by TestHoldcoRoatcePreferredCommonBasis + golden 35/35 live]** HoldCo-ROATCE preferred fix (the #24 follow-up). `analysis/valuation.py:compute_roatce_holdco` was preferred-INCLUSIVE and mislabeled — NI÷(total_equity−intangibles) — but ROATCE = return on tangible COMMON equity, with two offsetting errors (total NI vs NI-to-common; total vs common equity). Fixed to `(NI-to-common TTM) ÷ (common_equity − intangibles)`: added `net_income_to_common_ttm` (TTM of NetIncomeLossAvailableToCommonStockholdersBasic) to sec_client.get_latest_fundamentals; compute_roatce_holdco now subtracts preferred both sides + honors the cardinal rule (preferred present-but-unresolved → n/a). Golden re-pinned NON-circularly from tools/golden_handcheck.py (its ROATCE line corrected to NI-to-common÷common-TCE): JPM 19.0→19.83, BAC 13.1→14.84, WFC 13.84→14.97, C 7.6→7.71, USB 15.9→16.6 (documented MSR-excluded; hand-check reads 17.89 gross), HBAN 9.63→10.80, SFST 9.22 unchanged, PNC DISABLED (preferred unresolvable → n/a, like its tbvps). Note: FDIC engines were never affected — bank-SUB preferred (EQPP) ≈ 0 for every big-cap (preferred lives at the holdco).
25. **[DONE 2026-07-08 — commit 21442bd, deployed green; pinned by tests/test_valuation_quarterly_derive.py]** **`_derive_quarterly_value` returns YTD cumulative as a "quarter"** — `analysis/valuation.py:126-127` when prior quarter missing → overstated TTM NI → fair_ptbv/undervalued flag. Fix: return None on the gap (both callers, compute_roatce_4q + _normalized_earnings_factor, already skip a None quarter and annualize from the ones present); Q1 (YTD==quarter) and normal Q2-Q4 diffs unchanged.
26. **[DONE 2026-07-08 — pinned by TestQuarterlyFlowDecumulation (hand-computed + structural); JPM-live-verified]** **Quarterly yield/cost kinds mix annualized-YTD numerator with single-quarter avg denominator** — `ui/financials_statements.py:534-632, 1044-1059` (tens of bps off in rising-rate regimes; the deposit-cost section de-cumulates correctly). Fix: module-level `_decum_flow()` — Quarterly columns de-cumulate the YTD flow to the single quarter (Q1 = the YTD; else cur − prior-quarter YTD, date-keyed over the FULL 44q history so the first displayed column still reaches its prior) ×4, matching the single-quarter avg denominator; Annual unchanged (FY figure ×1); prior missing → "—" (never a mixed-span number). All 10 flow÷avg kinds converted (roatce/roate/roace/yield/yield2/netopex/costfunds/costdebt + core_roaa/core_roae via `_core_flow`, whose YTD effective-tax rate is a rate, not a flow). FTE-NIM add-on (:785) intentionally left YTD-annualized — it adjusts FDIC's YTD-annualized NIMY, internally consistent. Live JPM check: Q1 columns 0bp (identical by construction), interior quarters 3–8bp in 2025's flat regime, direction correct (rising costs → run-rate above YTD avg).
27. **[DONE 2026-07-08 — pinned by TestHistoricalsFailureNotCached]** **Historicals caches an empty DataFrame for 1h on transient FDIC error** — `ui/historicals.py:57-90` (process-global blank tab). Fix: `fetch_historical` no longer catches-and-returns-empty — a transient FDIC failure PROPAGATES (st.cache_data does not cache exceptions → next rerun retries); the caller shows the error. The legitimate no-rows-for-cert empty frame stays cacheable. Same "failures are never cached" class as #19.
28. **[DONE 2026-07-08 — commit b607bf3, deployed green; pinned by tests/test_capital_dynamics_equity.py]** **`or 0` coercion in capital dynamics** — `analysis/capital_dynamics.py:41-45` (missing EQTOT → equity 0 → garbage TBV/retention, capital alerts). Fix: skip the quarter when EQTOT is None (matches the existing missing-REPDTE skip); goodwill/intangibles keep `or 0` (an absent intangible legitimately means zero, unlike absent equity).
29. **[DONE 2026-07-09 — commit da47dc4, deployed green; pinned by TestMissingNimIsNaNotZero]** **Missing FDIC NIM renders "0.00%" reported + $0 scenario base** — `ui/rate_sensitivity.py:88-103` with `analysis/rate_sensitivity.py:93-99`. Fix: every scenario anchors on the current NIM (NII base = earning assets × NIM), so render_rate_sensitivity now guards on a missing NIMY (the shared anchor for all four tabs) and shows "unavailable" instead of running scenarios off a _safe()'d zero. The model is consumed only by this panel, so the single guard covers the displayed surface.
30. **[DONE 2026-07-09 — commit e4dac99, deployed green; pinned by TestNoPlaceholderSeed + TestHeadlineGuardOnMissingInputs]** **Placeholder-seeded "Warranted Price"** — `ui/valuation_model.py:320-331, 399` (tbvps=$20/eps=$2 placeholders make a headline fair price when SEC data missing). Fix: _derive_defaults returns None (not $2/$20) when EPS/shares can't be derived; the input widgets render empty; and the render path stops with an honest message before the DCF run when either headline input is missing (a user can still type values to model manually). Requiring TBV/share also guarantees a real share count, so every downstream per-share input is real.
31. **[DONE 2026-07-08 — pinned by TestConsensusRoatceNotRoe]** **Consensus "roatce" row compares street ROATCE to FDIC ROE** — `ui/valuation_model.py:916-917`. Fix: the row now compares against `compute_roatce(fdic_latest)` (annualized NETINC ÷ (EQTOT − INTAN), house convention); FDIC ROE (total-equity, no intangible deduction — a different, lower definition) removed from the comparison. compute_roatce=None → row skipped rather than compared cross-definition.
32. **[DONE 2026-07-09 — commit e7d4c0d, deployed green; pinned by TestPastDueLoansNotAssets]** **P3ASSET/P9ASSET ÷ LNLSNET presented as "past-due loans / total loans"** — `ui/credit_dynamics.py:105-109, 249-252` (past-due ASSETS over net-of-allowance loans). Fix: numerator → P3LNLS/P9LNLS (loans only), denominator → LNLSGR (gross loans, matching the FDIC-computed NPL ratio NCLNLSR in the same table). Field availability + divergence verified live on FDIC (BofA 2026-Q1: P3ASSET 5,567M vs P3LNLS 5,117M). Touches the timeline mapping (feeds the migration chart + pd_migration alert), the headline-card provenance, and the click-table rows. NOTE (follow-up): config.py:830/835 `past_due_30_89`/`past_due_90` peer metrics still map to P3ASSET/P9ASSET — a separate, unflagged surface (peer_comparison); left as-is (its assets-vs-loans intent is ambiguous) pending an owner call.
33. **[DONE 2026-07-08 — commit aa6aea2, deployed green; pinned by tests/test_credit_reserve_peer_median.py]** **Thin-reserves alert mixes computed coverage vs raw IDERNCVR peer median** — `analysis/credit_dynamics.py:211-219` vs 69-75. Fix: shared `_reserve_coverage()` helper (computed from LNATRESR/NCLNLSR, IDERNCVR fallback) drives BOTH the per-bank timeline and the peer median, so the two sides of the comparison can't diverge; per-bank behavior unchanged.
34. **[PARTIAL 2026-07-09 — commit 1fd93a6, deployed green; pinned by 3 availability cases in test_earnings_calendar_nonblocking]** **Calendar/KPI outages render "0 reporting" / "no earnings" instead of unavailable** — `ui/earnings.py:707-713, 759-762, 1110-1123` with `data/estimates.py:210-217` (the `cal_failed` guard can't trip). Fix: fetch_earnings_calendar returned [] for BOTH "no upcoming earnings" and "snapshot missing/unreadable" and never raised, so cal_failed could not trip. Added earnings_calendar_available() (pure snapshot-presence read, same non-blocking contract) and used it in the KPI bar — empty + no snapshot → "unavailable"; empty + snapshot present → honest "0". STILL OPEN: the upcoming-agenda tab's "No upcoming bank earnings found" message (ui/earnings.py ~1298) blends FMP + call sources too, so a snapshot-only check can't correctly diagnose it — needs an all-sources-down check.
35. **[DONE 2026-07-08 — commit 8a62b8c, deployed green; pinned by tests/test_financial_highlights_tbvps.py]** **TBVPS silently falls back to BVPS on date-miss** — `ui/financial_highlights.py:286-295` (goodwill exact-date vs equity nearest-date; use eq_date for all three). Fix: goodwill/other/incl (and the `_incl` drill-down field) now key on `eq_date`, the same balance sheet as equity; exact-date path unchanged.
36. **[DONE 2026-07-09 — owner chose short−long everywhere; pinned by TestSpreadConventionShortMinusLong + updated TestSlopeRegime]** **Spread sign convention split: Home short−long vs Macro/RateSens long−short** — `ui/macro.py:1339-1340, 1280, 1536-1537`, `ui/rate_sensitivity.py:156-164`, `data/fred_client.py:177-198`. Each internally consistent; same spread shows opposite signs across tabs. Owner call. Fix (all display-layer; raw FRED series + curve_regime/recession thresholds stay long−short internally): Macro health strip "2Y−10Y/3M−10Y" with negated values; rates board rows relabeled + whole series negated at fetch via `_SHORT_LONG_NEGATE` (latest/deltas/spark/z all self-consistent); curve-spreads chart negated with inversion shading flipped to the positive side ("above 0 = inverted"); Rate-Sens slope now 3M−5Y with flipped regime thresholds (negative=Steep, positive=Inverted) and the card/popup relabeled; recession-factor strings present short−long ("2Y-10Y inverted +0.75pp").
37. **[DONE 2026-07-09]** **`from app import …` fallback re-executes the whole script** — `ui/peer_rank.py:75, 88` (can never succeed; swallowed; delete). Verified: the functions DO exist in app.py, but importing app mid-render re-executes app.py's module-level code and its `st.set_page_config` (app.py:26) raises after the first Streamlit command, so the fallback always fell into the swallowed except — dead in prod, and a whole-script re-execution hazard if it ever "worked". Deleted both fallbacks (behavior-neutral: the one real caller, company_nav._peer_rank, always passes the cohort); empty cohort now renders the honest empty state.

### Security
38. **[DONE 2026-07-07 — pinned by tests/test_filings_escaping.py]** **Stored-XSS/HTML-breakage gap: AI filing summaries + item chips into unsafe_allow_html with only `_no_latex`** — `ui/filings.py:451-461, 507`. Every other feed surface escapes; also hrefs at :466-470 unescaped and not run through `is_safe_news_url`. (IAP caps blast radius; still the one unescaped external-text sink.) Fix: added `_safe()` (html.escape FIRST, then `$`→`&#36;` — correct order avoids double-escaping the entity); summary/primary/chips cells now use `_safe`; View/Index hrefs gated by `is_safe_news_url` (rejects javascript:/data: — empty host) and `html.escape`d. The two st.markdown summary expanders (:382,:537) are NOT unsafe_allow_html so they stay on `_no_latex` (markdown doesn't execute raw HTML).
39. **SEC-sourced profile strings unescaped in title_bar/ledger HTML** — `ui/bank_detail.py:326-330, 390-402` (incl. raw `href={web}`).
40. **[DONE 2026-07-07]** **Legacy `deploy/cloud-shell-deploy.sh:44-56` replays the 2026-06-24 secret outage if ever run** — pre-hardening silent-skip loop, secret list missing fmp/ffiec/external-access-password, downgrades resources, defaults to prod project. Add the required-secret loop or a "bootstrap only, never prod" abort. Fix: BOTH — (1) a never-prod abort at the top (refuses the prod project unless `ALLOW_MANUAL_PROD_DEPLOY=1`, pointing to the canonical GitHub Actions deploy; fires before any gcloud call), and (2) the secret loop now covers all 5 required (anthropic/fred/fmp/ffiec-username/ffiec-jwt) + optional external-access-password, failing loudly on any missing required secret — mirrors deploy.yml. Verified: `bash -n` + guard truth-table test.
41. **XSRF protection disabled** — `Dockerfile:37`, `.streamlit/config.toml:12`. Acceptable behind IAP; add a comment noting the IAP dependency.

### Ops / tests
42. **[DONE 2026-07-08 — live in Cloud Monitoring, user-created]** **No alerting on Cloud Run JOB failures** — `ops/setup_monitoring_alerts.sh` has only service 5xx. Jobs exit non-zero by careful design and nothing consumes it (refresh-prices 3-day-outage precedent). Add a log-based alert on failed job executions. Fix (live infra, created by the owner via gcloud 2026-07-08): metric-based alert policy "Cloud Run job execution failed" (`projects/ace-beanbag-486220-a8/alertPolicies/3762989835959672755`) on `run.googleapis.com/job/completed_execution_count` filtered `result="failed"`, grouped by job_name (per-job incidents), autoClose 30 min → email channel "KSK-Bank-Dashboard-alerts" (`notificationChannels/7155093753846383159`, chris@kskinvestors.com). Fires only on FAILED executions (after retries exhaust), so self-healing retries don't page. Note: email channels need NO verification (blank verificationStatus = not-applicable; SMS-only concept) — the pre-existing 5xx channel shows the same. Verified live: `policies list` shows both policies enabled=True.
43. **[DONE 2026-07-06 — commit pending, test-only]** **Rotted suite: `tests/test_macro_calendar.py`** — 3 failures; commit 74e35a8 added a `time` field the expected dicts lack. FRED-calendar guards effectively off. (Fix: added the real release times — prints "8:30 ET", FOMC "2:00 ET" — to the three exact-dict fixtures; pure test-drift, no product change.)
44. **[DONE 2026-07-06 — commit 870a285; infinite-tail time mock]** **Rotted suite: `tests/test_poll_events_budget.py`** — finite `time.time()` side_effect list exhausted by newer adapters (StopIteration). Budget-cutoff guard unverified.
45. **CI gap on the deploy path** — push-to-main runs only render-smoke pre-build + universe-coverage post-deploy (2 of 87 files); ci.yml is PR-only and deploys are direct pushes. The two rotted suites are the proof this keeps biting. Note: universe-coverage runs POST-deploy — it's an alarm, not a gate (in-file comment claims otherwise).
46. **[DONE 2026-07-08 — pinned by tests/test_trends_coverage_gate.py]** **Trends-grid write has no coverage threshold** — `jobs/refresh_trends.py:41-46` + `data/as_of_metrics.py:143-171` (partial FDIC response persists a shrunken grid under the stable key; contrast refresh_prices 90% / refresh_sod 95% gates). Fix: `quarterly_series` gates the PERSIST at `_MIN_GRID_COVERAGE=0.90` (measured baseline 2026-07-08: 99.7% of universe certs return data over the trailing window; newest quarter-end legitimately returns 0 pre-filing, which is why coverage counts banks-with-any-data) — below the gate the partial payload is returned but NOT written, so stale-but-complete keeps serving; refresh_trends exits 1 on `persisted=False` (pages via the #42 alert). SEC per-share grid got the same coverage computation but OBSERVE-ONLY (logs <90%, still persists) — its baseline is unmeasured; harden per the verify-baseline-before-arming rule after nightly logs show a stable baseline.

## P3 — minor / latent (condensed)

> **P3 BATCH CLOSED 2026-07-09** (3 parallel agents + main thread; 35 new pinning
> tests across tests/test_scraper_p3_guards.py, test_analysis_p3_guards.py,
> test_ui_p3_guards.py — all green with render smoke + regression suites):
> scraper period-mix guards (composition-fallback, unrealized/CET1, NIM band) ·
> 8-K _table_rows column alignment · bank_mapping phrase-search single-encoding ·
> econ_calendar window-keyed cache · screen_engine/peer_groups NaN guards ·
> capital_dynamics early-return key + ui _absmax NaN scale guard · valuation
> winsor docstring · rate_sensitivity backtest copy (min-8 constant) ·
> bank_detail Ann.-YTD labels + INTAN fallback pin · financial_highlights honest
> "—" for missing equity · macro ET "Today" highlight · chart_style docstring +
> formatting abs()-tier fix · docker-compose 8501→8080 · deploy.yml pyflakes gate
> now covers tools/ (baseline verified first) · **falsy-zero (owner call: $0 is
> real)** in analysis/valuation cre_to_capital + deposit_dynamics mix pcts.
> **Still open (deliberate):** ui/earnings.py items (#upload-UX, "0 ($000)" twin,
> cache-key tickers — earnings session's lane) · data/estimates.py next-earnings
> direction · data/events/ir_site.py Q4-snapshot clobber (news session's lane) ·
> ui/home.py overlay-stretch + calendar-escape (Home session's lane) ·
> rate_sensitivity repricing-slider wiring (behavior change, needs care) ·
> valuation cost-of-funds denominator (measure first) · dead-code deletions +
> release_metrics._table_rows twin (quiet-window items) · slow test suites ·
> run-job.yml (awaits owner approval of the credentialed CI pathway).

- **[DONE 2026-07-08 — commit efba515, deployed green; pinned by tests/test_sec_client_restatement.py]** `data/sec_client.py:433-437` — restatement dedup keeps arbitrary (often original) value; fix `sort_values(["end","filed"])`. Done: sort by (end, filed) na_position="first", keep="last", re-sort end-desc for the newest-first contract.
- `data/sec_filing_scraper.py:1311-1321` — credit-quality composition fallback divides current-Q ACL by prior-FY loan total, unflagged. `:2312-2337` — unrealized/CET1 can mix periods. `:1676-1690` — NIM fallback lacks sanity band + fragile positional triplet assumption.
- `data/sec_earnings_8k.py:141-156` — `_table_rows` drops empty cells; blank latest-quarter cell serves prior period as current.
- `data/bank_mapping.py:549-563` — FDIC phrase search double-URL-encoded (tier mostly fails → safe None).
- **[DONE 2026-07-09 — pinned by TestWireUpgradeUniqueCollision (verified: guard-off run reproduces the IntegrityError batch rollback)]** `data/events/store.py:240-283` — wire-upgrade UPDATE can hit UNIQUE and roll back the whole adapter batch each cycle for 5 days. Fix: the upgrade branch pre-checks whether the incoming wire copy already exists as its own (source, external_id) row — if so, nothing to upgrade to; record it as the content key's best and skip. Urgency was amplified by P2 #21 (full-lookback re-scans re-emit the colliding wire copy every cycle).
- `data/price_cache_store.py:190-199` — SQLite INSERT OR REPLACE wipes nightly derived columns (dev-only; the `last_seen_published` str-from-SQLite note is moot — deleted with P2 #21).
- `data/fred_client.py:50` — "DXY" is not a FRED series (dead entry, hourly "empty" log noise; real id DTWEXBGS).
- `data/econ_calendar.py:28,109-135` — cache key omits window params (latent).
- `data/events/ir_site.py:738-746` — Q4-calls snapshot clobbered with {} on transient failure (self-heals in 30 min).
- `data/estimates.py:120-139` — next-earnings fallback picks farthest future row.
- `ui/earnings.py:675-697` — cache key ignores tickers arg; `:1893-1918` — st.rerun() wipes bulk-upload results; `:617-620` + `ui/financial_highlights.py:501,517` — "0 ($000)" for missing equity in provenance.
- `ui/bank_detail.py:190-191` — "LTM" label on YTD-annualized ratios (true only at Q4); `:120-129` — ROATCE fallback INTAN vs engine INTANGW basis discontinuity.
- `ui/home.py:969-971` — overlay SVG time-stretches shorter stale series; `:702` — one unescaped calendar cell.
- `ui/rate_sensitivity.py:228-233` — repricing-speed slider ignored by default tab; `:1070` — truthiness drops vline at exactly 0.0; `:757-760` — copy hardcodes "20 quarters" (gate is ≥8).
- `ui/macro.py:1102` — "Today" highlight keyed to UTC date vs ET rows (wrong after ~8pm ET).
- `ui/capital_dynamics.py:324` — `.max() or 0` doesn't neutralize NaN.
- `analysis/screen_engine.py:44-50` + `analysis/peer_groups.py:183-225` — NaN passes `is not None` → scored FAIL / 0th percentile (latent; activates on any pandas round-trip).
- `analysis/valuation.py:289 vs 311` — docstring 2× vs code 3× winsor cap; `:452-459` — cost-of-funds fallback denominator too narrow; `:401-404,431` + `analysis/deposit_dynamics.py:112-114` — falsy-zero renders genuine $0 as n/a (drops zero-CRE banks from CRE classification).
- `analysis/capital_dynamics.py:327-331` — early-return dict key `buyback_capacity_k` vs `_usd` (unreachable today).
- `.github/workflows/run-job.yml:13-20` — manual-trigger list covers 7 of 16 jobs; still on auth@v2.
- `docker-compose.yml` — port 8501 vs PORT=8080 entrypoint; dir mounted onto file path (local-dev trap).
- `deploy.yml:49` — pyflakes gate omits tools/ (jobs/verify_metrics imports tools.verify_metrics at runtime).
- Dead code: `ui/watchlist.py`, `ui/balance_sheet_table.py` (zero importers), `render_overview_table`/`render_column_selector`, `TAB_LABELS` (config.py:561); `"ibkr"` source naming now means FMP price provider.
- `utils/chart_style.py:3` — stale "Premium dark theme" docstring; `format_value` doesn't abs() before tier checks (cosmetic).
- Slow "unit" suites: test_events_dedup_and_tagging 10.5 min, test_poll_events_budget ~80s (real sleeps) — gate-hostile as-is.
- Doc staleness (conservative direction): AUDIT-PRODUCTION-READINESS Gap A still says smoke "flipped to BLOCKING" (reverted to observe-only 2026-06-17); two "remaining open" items in AUDIT-2026-06-11 (bps NIM fixtures, freshness-policy doc) are actually done.

---

## Verified clean (positive assurance)

- **Prior audits fully honest**: every item claimed done in AUDIT-2026-06-11 and AUDIT-PRODUCTION-READINESS verified present in code (21 A-items, P1/P2 infra, calibrated-constant pins, all prod-readiness gaps incl. base-image digest pin). Only two still-open carryovers: app-wide fetch-failure staleness badge (P1 remainder, partially mitigated) and the deliberately-deferred name-normalizer unification.
- **Security**: no secrets in any tracked file or reachable history; ffiec JWT tool confirmed untracked; access gate uses hmac.compare_digest, password never in URL/logs, state server-side; all SQL parameterized; no shell=True; every requests call has a timeout; no public-ACL GCS writes; DB password masked in CI logs.
- **Deploy contracts**: image-sync loop covers all 16 jobs (PASS); secret-mount guard hard-fails and is regex-pinned by a test (PASS); concurrency group present (PASS); requirements.txt 92 exact pins, zero ranges (PASS); Dockerfile base pinned by sha256 digest (PASS); refresh_capital ≤8 req/s thread-safe throttle (PASS); snapshot writes are atomic upserts keeping last-good on failure (PASS).
- **Pyflakes**: zero undefined names repo-wide (in-scope dirs). **Pytest collection**: 1,277 tests, zero collection errors. **Streamlit stubs**: no missing st.* APIs after 8209015+62a6fd1 (verified empirically).
- **Warm contracts**: every Home overlay/ETF timeframe maps into `_OVERLAY_PERIODS`; yield-spread negation on Home correct including flipped ranges; FMP EOD stamps real trading date; COMPANY_NAV ↔ renderer registries fully in sync; no duplicate widget keys; no bank special-casing; FDIC $K scaling correct at every checked touchpoint; config METRICS integrity clean.

## Suggested remediation order

1. P0 #1-2 (two label/field fixes, ship same day, add pinned tests).
2. P1 #17 (one-line list extension in deploy.yml — incident-class guard), #18 (tools script guard), #3-5 (data layer), #42 (job-failure alert).
3. P1 UI cluster #6-16 (mostly small, high-visibility fixes; verify in incognito per house rule).
4. P2s in file-cluster batches (financials_statements basis fixes together; valuation TCE convention decision first — pick ONE TCE definition and pin it with a test).
5. Fix the 2 rotted suites (#43-44), then extend the deploy-path gate with the fast, high-value suites (respect verify-baseline-before-arming-gate: green streak first).

## Coverage limits (what a deeper pass could still cover)

- ui/macro.py's 25 unsafe-HTML sites sampled, not exhaustively traced.
- Live GCP state not verified (actual scheduler inventory for macro/capital/company-financials, console-created alert policies, job task timeouts) — one `gcloud scheduler jobs list` + alert-policy list would close this.
- Not deeply reviewed: analysis/capital_return.py and ownership_analytics internals, FFIEC `_dep_quarterly_cost_rate` internals, network-dependent test suites (live_audit, smoke_live, golden_dataset) not executed.
- Dependency CVEs: pins are 2026-era, beyond model knowledge — rely on the existing dependabot + consider a pip-audit CI step.
