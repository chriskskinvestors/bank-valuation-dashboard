name: Refresh NIC bulk mirror

# NPW (ffiec.gov/npw) sits behind Cloudflare bot management that 403s Cloud
# Run egress IPs outright — production can never download from it (prod logs
# 2026-07-14). GitHub-hosted runners use different egress, so this workflow
# mirrors everything the app needs from NPW into GCS:
#   1. the two NIC bulk zips (nic_bulk/ — Corporate Structure sub-tab); the
#      zips change slowly, so monthly matches nic_client's 30-day TTL
#   2. the latest filed quarter of FR Y-9C facsimile PDFs (y9c/ — Recent
#      Documents → Regulatory Filings "Download via dashboard"); the extra
#      cron on the 16th of Feb/May/Aug/Nov lands right after each Y-9C
#      filing deadline (40-45 days after quarter-end) so a new quarter is
#      mirrored within days of the facsimiles appearing, and the monthly
#      run catches late filers (already-mirrored PDFs are skipped)
# If Cloudflare ever blocks GitHub's IPs too, this fails loudly (red run)
# and the fallback is running tools/refresh_nic_bulk.py and
# tools/refresh_y9c_mirror.py from any unblocked machine (e.g. the dev
# box); nic_client keeps serving the stale mirror in the meantime.
on:
  schedule:
    - cron: "17 6 3 * *"          # 06:17 UTC on the 3rd of each month
    - cron: "17 6 16 2,5,8,11 *"  # just after each FR Y-9C filing deadline
  workflow_dispatch:

env:
  PROJECT_ID: ace-beanbag-486220-a8
  REGION: us-central1
  SERVICE: bank-dashboard
  GCS_BUCKET: ksk-bank-dashboard-data
  SA_EMAIL: bank-dashboard-sa@ace-beanbag-486220-a8.iam.gserviceaccount.com

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write   # Required for Workload Identity Federation
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: github-deployer@${{ env.PROJECT_ID }}.iam.gserviceaccount.com

      - name: Fetch bulk zips from NPW, validate, upload to the GCS mirror
        run: |
          # Only what the two refresh tools' import chains need — not the
          # full requirements.txt (this runs on a monthly cron).
          python -m pip install --quiet google-cloud-storage pandas requests
          python -m tools.refresh_nic_bulk

      # Latest-quarter FR Y-9C facsimiles for every likely filer. First run
      # of a new quarter fetches ~370 PDFs at 30s spacing (~3h); later
      # runs skip already-mirrored objects and finish in minutes.
      - name: Mirror latest-quarter FR Y-9C facsimile PDFs
        run: python -m tools.refresh_y9c_mirror

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v3
        with:
          project_id: ${{ env.PROJECT_ID }}

      # Prove the PRODUCTION read path end-to-end: run the live service
      # image as a Cloud Run job with a wiped /tmp (a brand-new instance)
      # and require it to serve + parse both bulk files from the mirror
      # just uploaded. Fails this workflow loudly if a fresh prod instance
      # couldn't render the Corporate Structure tab.
      - name: Verify fresh-instance read path (empty /tmp → GCS mirror)
        run: |
          IMAGE=$(gcloud run services describe "${{ env.SERVICE }}" \
            --region="${{ env.REGION }}" \
            --format='value(spec.template.spec.containers[0].image)')
          echo "Verifying with service image: $IMAGE"
          gcloud run jobs deploy nic-mirror-verify \
            --image="$IMAGE" \
            --region="${{ env.REGION }}" \
            --service-account="${{ env.SA_EMAIL }}" \
            --command=python \
            --args=-m,tools.verify_nic_mirror \
            --set-env-vars="GCS_BUCKET=${{ env.GCS_BUCKET }},GCLOUD_PROJECT=${{ env.PROJECT_ID }}" \
            --memory=2Gi \
            --task-timeout=600 \
            --max-retries=0 \
            --quiet
          gcloud run jobs execute nic-mirror-verify \
            --region="${{ env.REGION }}" \
            --wait
